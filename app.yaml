runtime: python39
entrypoint: gunicorn -b :$PORT main:app

env: standard
instance_class: F1

handlers:
- url: /.*
  script: auto
  secure: always

env_variables:
  FLASK_ENV: 'production'
  GOOGLE_CLOUD_PROJECT: 'assignment-1-417320'
